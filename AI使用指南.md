Claude三种工作模式：shift+tab转换模式
?for shutcuts     常规
accept edits on   自动改文件
plan mode on      只讨论不修改

claude常用命令

/init 可初始化项目，自动生成项目文档、代码规范和技术栈等信息
/resume 继续上一轮对话
/compact 压缩上下文
/btw 可在不影响主代理思考时额外提问
/clear 清空当前对话上下文
/memory 显示所有加载的 Claude.md 文件
/add-dir 扩展其能访问的文件夹
/cost 查看 tokens 使用量
/doctor 诊断当前开发环境、项目状态、cc自身运行状况
/export 快速导出当前对话
/model 切换使用模型
/plan 切换 plan 模式输出结构化任务
/rename 重命名当前对话
/review 对指定文件的内容进行多维度专业审查
/rewind 回退上一次使用的 slash 命令
/task 查看当前后台运行的任务


######
`子代理团队开发 /agent` 一个ai编写一个审查一个测试
`插件 /plugin`
`skill(提供示例效果大幅提升)由skill文件夹中SKILL.md中的描述触发（可关闭）`
`生成plan.md并将其中规则写入Claude.md（全局变量每条命令执行前都会执行就是规则）`
#####

esc退出，不满意直接说回退到上一次(双击esc亦可)
终端输入claude -r可在历史会话中选择一个，-c上一次会话
自定义 /命令
hook用于各种命令执行后的操作


CLAUDE.md(规则)
SKILL(技能)
MCP(连接外部世界)
Subagent(子代理)
Hook(事件自动触发器)
Plugin(能力打包包括 SKILL,MCP,Subagent,Hook)


新手入门：先从 Skill 和 CLAUDE.md 开始，成本最低、见效最快
提升效率：加装 串口 MCP + SSH MCP，消除复制粘贴日志的重复劳动
团队协作：把规范和工具打包成 Plugin，统一团队开发标准
大型项目：启用 Subagent，拆分模块开发、代码审查、日志分析等任务，并行推进
自动化进阶：配置 Hook，把编译、检查、测试等流程完全自动化






1、掌握“背景+任务+要求”要素公式,并加上“请分步思考，先分析核心问题，再拆解解决步骤，最后给出具体方案，每一步说明理由”。
 你可以自己运行这个命令，出现错误后自己在关键位置打印得到错误信息并完成修改
2、利用多个 AI 评审，让他们分别阅读代码给出文档并互评后汇总
3、自己对项目没有足够的了解时用 Plan 模式可帮助你生成需求文档
4、检查一下代码，增强可移植性和健壮性。方便后面换开发板能快速上手和正常使用
5、帮我看看在保证健壮性和可移植性的前提下代码有哪里性能可以优化的地方吗，先不修改列出来我看看



`规则`
1、我会提供充足上下文：如背景、相关文档等, 帮助明确需求、功能、技术栈和做法等，如果你觉得我给的不够充分你必须问我，然后我给你补充，如果我的想法比较不妥或局限，你可以给我提供几个思路和方案。

2、强制停止任何猜测行为，对于重大决策、添加推测性代码和新功代码时你应该先分析相关代码，并给出建议和帮助我确认好方向，最后得到我确认才能动手。

3、强制只写能解决当前问题的最小量代码，只碰绝对必要代码。

4、将所有模糊的任务强制转化为可验证的成功标准，在关键处、易错处添加调试并自己读取相关日志，自主完成测验及修复。

5、当用户让你了解一下这个项目时，你应该生成一份项目记忆文档，并在后续运行时及时更新。

6、失败处理流程，当执行/编译/运行失败时，按以下顺序处理：
   - 第一步：完整读取报错信息，定位根因（禁止只看最后一行）
   - 第二步：分析可能的原因，给出修复方案
   - 第三步：用户确认方案后再修改
注意禁止试错式开发，不得反复瞎改碰运气，每次修改必须有明确的分析依据。排错过程中发现的新问题，如果超出当前任务范围，先暂停并告知，确认是否扩展任务。

7、回复格式要求，代码改动说明必须包含：
   - 修改了哪个文件（路径）
   - 修改位置（函数名或大致行号范围）
   - 修改原因和思路
   - 可能的影响面

8、 不确定的地方必须明确说"不确定"，禁止编造理由或假装验证过。超出能力范围或知识边界的问题，如实说明并给出可行的替代建议。没看懂的代码先问清楚，不要硬着头皮改。

9、每次动手改代码前，快速过一遍：
- [ ] 相关上下文代码都读过了吗？
- [ ] 需求明确吗？能写出成功标准吗？
- [ ] 是不是只改了必要的最小范围？
- [ ] 改完自测并可行了吗？
- [ ] 调试代码清理干净了吗？
- [ ] 记忆文档需要更新吗？

/readme 更新一下readme
1. 项目介绍：项目定位、核心功能、解决的问题、适用场景、项目优势
2. 环境依赖：系统要求、软件版本、依赖框架/库、环境配置前置条件
3. 快速安装/部署：源码拉取、依赖安装、环境初始化命令
4. 使用步骤：分步骤清晰罗列启动、运行、调用、测试完整流程
5. 项目架构：架构图+文字说明，拆解目录结构、核心模块、数据流、职责划分
6. 配置说明：核心配置文件、关键参数释义、自定义配置方法
7. 常见问题与报错解决：收录高频报错、问题现象、原因分析、完整解决方案
8. 项目更新日志：版本迭代记录、新增功能、问题修复、优化内容
9. 开源/使用说明：开源协议、使用规范、禁止行为、贡献指南、联系方式
10. 内容务实：无空话套话，所有步骤、方案可直接复制执行，参数、命令精准
11. 排版清晰：层级分明，使用标题、列表、代码块分区展示，重点内容加粗
12. 适配新手：规避专业黑话，关键步骤附带注释，零基础可上手操作
